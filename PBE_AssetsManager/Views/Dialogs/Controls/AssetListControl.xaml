<UserControl x:Class="PBE_AssetsManager.Views.Dialogs.Controls.AssetListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PBE_AssetsManager.Views.Dialogs.Controls"
             xmlns:info="clr-namespace:PBE_AssetsManager.Info"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <Border Style="{StaticResource SettingsCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Assets List Header -->
            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="📋" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                    <StackPanel>
                        <TextBlock Text="Assets List" 
                                     FontSize="16" 
                                     FontWeight="SemiBold"
                                     Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Select an asset to preview" 
                                     FontSize="12" 
                                     Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Search Box -->
                <Grid>
                    <Border Background="{StaticResource DarkBackground}"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"/>
                    <TextBox x:Name="txtSearch"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="12,10"
                             Foreground="{StaticResource TextPrimary}"
                             FontSize="14"
                             TextChanged="TxtSearch_TextChanged"
                             GotFocus="TxtSearch_GotFocus"
                             LostFocus="TxtSearch_LostFocus"/>
                    <TextBlock Text="🔍 Search an asset..."
                               x:Name="txtSearchPlaceholder"
                               IsHitTestVisible="False"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0">
                    </TextBlock>
                </Grid>
            </StackPanel>

            <!-- Assets ListBox -->
            <Border Grid.Row="1"
                    Background="{StaticResource DarkBackground}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="6">
                <ListBox x:Name="listBoxAssets"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="8"
                         SelectionChanged="listBoxAssets_SelectionChanged"
                         FontFamily="Consolas, Courier New" 
                         FontSize="13"
                         Foreground="{StaticResource TextPrimary}">
                    <ListBox.Resources>
                        <DataTemplate DataType="{x:Type info:AssetInfo}">
                            <TextBlock Text="{Binding Name, StringFormat='  {0}'}" Margin="0,2"/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type sys:String}">
                            <TextBlock Text="{Binding}" FontWeight="Bold" Margin="0,4,0,2"/>
                        </DataTemplate>
                    </ListBox.Resources>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackground}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </Grid>
    </Border>
</UserControl>
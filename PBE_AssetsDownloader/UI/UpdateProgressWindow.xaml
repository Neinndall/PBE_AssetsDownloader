<Window x:Class="PBE_AssetsDownloader.UI.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Update" Height="150" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock" Grid.Row="0" Margin="0,0,0,10"
                   HorizontalAlignment="Center" TextWrapping="Wrap"
                   Text="Initializing download..."/>

        <ProgressBar x:Name="DownloadProgressBar" Grid.Row="1" Height="20" Minimum="0" Maximum="100"/>
    </Grid>
</Window>
<Window x:Class="PBE_AssetsManager.UI.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PBE_AssetsManager.UI.Dialogs"
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        mc:Ignorable="d"
        Title="Confirmation" Height="Auto" Width="350"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Background="{StaticResource CardBackground}" CornerRadius="5" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource AccentBlue}" CornerRadius="5,5,0,0" MouseDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <materialIcons:MaterialIcon x:Name="iconType" Kind="QuestionMarkCircle" VerticalAlignment="Center" Margin="10,0,5,0" Foreground="White"/>
                    <TextBlock Grid.Column="1" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" Margin="5,10" Foreground="White"/>
                </Grid>
            </Border>

            <TextBlock x:Name="textBlockMessage" Grid.Row="1" TextWrapping="Wrap" Margin="15,20" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15">
                <StackPanel x:Name="YesNoButtons" Orientation="Horizontal">
                    <Button x:Name="btnYes" Content="Yes" Style="{StaticResource ModernButton}" Width="80" Margin="0,0,10,0" Click="btnYes_Click"/>
                    <Button x:Name="btnNo" Content="No" Style="{StaticResource SecondaryButton}" Width="80" Click="btnNo_Click"/>
                </StackPanel>
                <Button x:Name="btnOk" Content="OK" Style="{StaticResource ModernButton}" Width="80" Click="btnOk_Click" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
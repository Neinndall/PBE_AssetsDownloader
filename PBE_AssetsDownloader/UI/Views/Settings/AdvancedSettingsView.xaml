<UserControl x:Class="PBE_AssetsDownloader.UI.Views.Settings.AdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <!-- Page Header -->
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="Advanced Settings" 
                         FontSize="28" 
                         FontWeight="Light" 
                         Foreground="{StaticResource TextPrimary}"
                         Margin="0,4,0,0"/>
            <TextBlock Text="Performance and advanced configuration options" 
                         FontSize="14" 
                         Foreground="{StaticResource TextMuted}"
                         Margin="0,2,0,0"/>
        </StackPanel>

        <!-- JSON File Monitoring Card -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <materialDesign:MaterialIcon Kind="FileDocument" 
                                                   Foreground="{StaticResource AccentBlue}"
                                                   FontSize="20" 
                                                   Margin="0,0,12,0" 
                                                   VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="JSON File Monitoring" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Manage the list of JSON files and directories to monitor for updates" 
                                     FontSize="12" 
                                     Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>

                <ListBox x:Name="JsonFilesListBox"
                         Background="{StaticResource DarkBackground}"
                         BorderBrush="{StaticResource BorderColor}"
                         BorderThickness="1"
                         Foreground="{StaticResource TextPrimary}"
                         Height="250"
                         Margin="0,0,0,12"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.Resources>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.Resources>
                </ListBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnAddJsonFile" Content="Add" Style="{StaticResource ModernButton}" Width="80" Margin="0,0,8,0" Click="btnAddJsonFile_Click"/>
                    <Button x:Name="btnEditJsonFile" Content="Edit" Style="{StaticResource SecondaryButton}" Width="80" Margin="0,0,8,0" Click="btnEditJsonFile_Click"/>
                    <Button x:Name="btnRemoveJsonFile" Content="Remove" Style="{StaticResource DestructiveButton}" Width="80" Click="btnRemoveJsonFile_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Diff History Card -->
        <Border Style="{StaticResource SettingsCard}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <materialDesign:MaterialIcon Kind="History"
                                                   Foreground="{StaticResource AccentPurple}"
                                                   FontSize="20"
                                                   Margin="0,0,12,0"
                                                   VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Difference History"
                                     FontSize="18"
                                     FontWeight="SemiBold"
                                     Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Review past JSON file comparisons"
                                     FontSize="12"
                                     Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>

                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,1,0,1" Padding="8,6">
                        <TextBlock Text="File Name" FontWeight="Medium" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="1" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,1">
                        <TextBlock Text="|" FontWeight="Medium" Foreground="{StaticResource BorderColor}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="2" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,1,1" Padding="8,6">
                        <TextBlock Text="Timestamp" FontWeight="Medium" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>

                <ListView x:Name="DiffHistoryListView"
                                      Background="{StaticResource DarkBackground}"
                                      BorderBrush="{StaticResource BorderColor}"
                                      BorderThickness="1,0,1,1"
                                      Foreground="{StaticResource TextPrimary}"
                                      Height="250"
                                      Margin="0,0,0,12"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Border" Background="Transparent" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="5" SnapsToDevicePixels="true">
                                            <GridViewRowPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackground}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                            <GridViewColumn Header="File Name" Width="320" DisplayMemberBinding="{Binding FileName}"/>
                            <GridViewColumn Header="Timestamp" Width="320" DisplayMemberBinding="{Binding Timestamp}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnViewDiff" Content="View Diff" Style="{StaticResource ModernButton}" Width="100" Margin="0,0,8,0" Click="btnViewDiff_Click"/>
                    <Button x:Name="btnClearHistory" Content="Clear History" Style="{StaticResource DestructiveButton}" Width="130" Click="btnClearHistory_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>